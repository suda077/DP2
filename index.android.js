import {
  AppRegistry,
} from 'react-native';
// const Setup = require('./js/pages/setup');
// AppRegistry.registerComponent('DP2', () => Setup.Root);

import Root from './src/scripts/Root.js';
AppRegistry.registerComponent('DP2', () => Root);
